{
  "name": "keyspace",
  "private": true,
  "description": "a space for key",
  "version": "1.1.0",
  "license": "0BSD",
  "scripts": {
    "build": "gatsby build",
    "develop": "gatsby develop",
    "deploy": "npm run clean && gatsby build --prefix-paths && rm -rf ./docs/* && mv ./public/* ./docs",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,json,md}\"",
    "start": "npm run clean && npm run develop",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "test": "echo \"Write tests! -> https://gatsby.dev/unit-testing\" && exit 1"
  },
  "dependencies": {
    "gatsby": "^4.16.0",
    "gatsby-remark-smartypants": "^5.16.0",
    "gatsby-source-filesystem": "^4.16.0",
    "gatsby-transformer-remark": "^5.16.0",
    "react": "^18.1.0",
    "react-dom": "^18.1.0"
  },
  "devDependencies": {
    "prettier": "^2.6.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/keytrain/keyspace"
  },
  "bugs": {
    "url": "https://github.com/keytrain/keyspace/issues"
  }
}
